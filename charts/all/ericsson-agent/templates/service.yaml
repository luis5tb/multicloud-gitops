apiVersion: v1
kind: Service
metadata:
  name: {{ include "ericsson-agent.fullname" . }}
  labels:
    {{- include "ericsson-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ericsson-agent.selectorLabels" . | nindent 4 }}

