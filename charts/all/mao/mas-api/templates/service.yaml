apiVersion: v1
kind: Service
metadata:
  name: {{ include "mas-api.fullname" . }}
  labels:
    {{- include "mas-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mas-api.selectorLabels" . | nindent 4 }}
